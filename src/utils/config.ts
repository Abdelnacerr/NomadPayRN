import {Dimensions} from 'react-native';

export const colors = ['#867EB1', '#FF4D4D', '#040F01'];
export const dimensions = Dimensions.get('window');
export const PAGE_WIDTH = dimensions.width;
export const PAGE_HEIGHT = dimensions.height;
